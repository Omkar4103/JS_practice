L7 Takeaways
1. The built in clipboard object allows us to manipualte the clipboard with javascript
2. Blobs (Binary large object) represent file as an object
3. Mime type is a string convention to describe a particular data type

L8 Takeaways
1. APIs are Fns exposed by a software. We use APIs to interact with the software
2. Programming APIs are consumed within the porgramming languaeg itsef, 
whereas Web APIs are consumed via HTTP
3. HTTP comes with 2 parts: the request and response. 
Request is sent by the browser; response is sent by the server
4. HTTP headers contain metadata about the request/response, 
while the HTTP body contains the payload
